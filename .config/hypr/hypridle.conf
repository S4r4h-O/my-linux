# Hypridle

general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

# turn off screen faster if session is already locked
# (disabled by default)
# listener {
#     timeout = 30
#     on-timeout = pidof hyprlock && hyprctl dispatch dpms off
#     on-resume = pidof hyprlock && hyprctl dispatch dpms on
# }

# Warn
listener {
    timeout = 540
    on-timeout = notify-send "💤 Idle Warning" "You are idle!"
    on-resume = notify-send "👋 Welcome Back" "Hello!"
}

# Screenlock
listener {
    timeout = 600
    on-timeout = loginctl lock-session
    # on-resume = notify-send "🔓 System" "Unlocked"
}

# Turn off screen 
# (disabled by default)
# listener {
#     timeout = 630
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }

# Suspend (disabled by default)
# listener {
#     timeout = 1200
#     on-timeout = systemctl suspend
#     on-resume = notify-send "👋 Welcome Back" "Hello!"
# }
